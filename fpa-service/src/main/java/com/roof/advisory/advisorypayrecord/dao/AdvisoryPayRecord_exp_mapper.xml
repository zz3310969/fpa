<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.roof.advisory.advisorypayrecord.dao">
	<sql id="columnsAs">
		t1.id as id, t1.order_id as orderId, t1.fee as fee, t1.platform as platform, t1.record_type as recordType, t1.prepay_id as prepayId, t1.request_data as requestData, t1.response_data as responseData
	</sql>
	
	<select id="selectAdvisoryPayRecordPage" resultType="com.roof.advisory.advisorypayrecord.entity.AdvisoryPayRecordVo">
		select
		<include refid="columnsAs"/>
		from
		z_advisory_pay_record t1
		join
		(SELECT
		id
		from
		z_advisory_pay_record
		where 1=1
		<include refid="conds" />
		order by id desc
		limit #{firstrownum}, #{limit}) t2
		where t1.id = t2.id
	</select>
	
	<select id="selectAdvisoryPayRecordCount" resultType="java.lang.Long">
		select
		count(id)
		from z_advisory_pay_record
		where 1=1
		<include refid="conds" />
	</select>
</mapper>