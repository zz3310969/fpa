<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.roof.advisory.advisoryorderrecord.dao">
	<sql id="columnsAs">
		t1.id as id, t1.order_id as orderId, t1.event_type as eventType, t1.event as event, t1.event_time as eventTime, t1.event_note as eventNote
	</sql>
	
	<select id="selectAdvisoryOrderRecordPage" resultType="com.roof.advisory.advisoryorderrecord.entity.AdvisoryOrderRecordVo">
		select
		<include refid="columnsAs"/>
		from
		z_advisory_order_record t1
		join
		(SELECT
		id
		from
		z_advisory_order_record
		where 1=1
		<include refid="conds" />
		order by id desc
		limit #{firstrownum}, #{limit}) t2
		where t1.id = t2.id
	</select>
	
	<select id="selectAdvisoryOrderRecordCount" resultType="java.lang.Long">
		select
		count(id)
		from z_advisory_order_record
		where 1=1
		<include refid="conds" />
	</select>
</mapper>