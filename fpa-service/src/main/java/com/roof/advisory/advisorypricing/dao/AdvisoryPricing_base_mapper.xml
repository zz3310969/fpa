<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.roof.advisory.advisorypricing.dao">

	<sql id="columns">
		id, app_id, consultant_id, molde_id, fix_type, unit, original_price, current_price, introduction, status, state, validity_start_time, validity_end_time
	</sql>

	<sql id="columns_as">
		id as id , app_id as appId , consultant_id as consultantId , molde_id as moldeId , fix_type as fixType , unit as unit , original_price as originalPrice , current_price as currentPrice , introduction as introduction , status as status , state as state , validity_start_time as validityStartTime , validity_end_time as validityEndTime 
	</sql>


	<sql id="vals">
		#{id}, #{appId}, #{consultantId}, #{moldeId}, #{fixType}, #{unit}, #{originalPrice}, #{currentPrice}, #{introduction}, #{status}, #{state}, #{validityStartTime}, #{validityEndTime}
	</sql>
	<sql id="conds">
		<if test="id != null">
			and id = #{id}
		</if>
		<if test="appId != null">
			and app_id = #{appId}
		</if>
		<if test="consultantId != null">
			and consultant_id = #{consultantId}
		</if>
		<if test="moldeId != null">
			and molde_id = #{moldeId}
		</if>
		<if test="fixType != null and fixType != ''">
			and fix_type = #{fixType}
		</if>
		<if test="unit != null and unit != ''">
			and unit = #{unit}
		</if>
		<if test="originalPrice != null">
			and original_price = #{originalPrice}
		</if>
		<if test="currentPrice != null">
			and current_price = #{currentPrice}
		</if>
		<if test="introduction != null and introduction != ''">
			and introduction = #{introduction}
		</if>
		<if test="status != null">
			and status = #{status}
		</if>
		<if test="state != null">
			and state = #{state}
		</if>
		<if test="validityStartTime != null">
			and validity_start_time = #{validityStartTime}
		</if>
		<if test="validityEndTime != null">
			and validity_end_time = #{validityEndTime}
		</if>
	</sql>

	<insert id="saveAdvisoryPricing" parameterType="com.roof.advisory.advisorypricing.entity.AdvisoryPricing" useGeneratedKeys="true" keyProperty="id">
		<selectKey keyProperty="id" resultType="Long" order="AFTER">
			select last_insert_id()
		</selectKey>
		insert into z_advisory_pricing (<include refid="columns" />) 
		values (<include refid="vals" />)
	</insert>

	<delete id="deleteAdvisoryPricing">
		delete from z_advisory_pricing
		where id=#{id} 
	</delete>

	<delete id="deleteByExampleAdvisoryPricing">
		delete from z_advisory_pricing
		where 1=1
		<include refid="conds" />
	</delete>

	<update id="updateAdvisoryPricing">
		update z_advisory_pricing
		<set>
			id=#{id}, 
			app_id=#{appId}, 
			consultant_id=#{consultantId}, 
			molde_id=#{moldeId}, 
			fix_type=#{fixType}, 
			unit=#{unit}, 
			original_price=#{originalPrice}, 
			current_price=#{currentPrice}, 
			introduction=#{introduction}, 
			status=#{status}, 
			state=#{state}, 
			validity_start_time=#{validityStartTime}, 
			validity_end_time=#{validityEndTime}
		</set>
		where id=#{id} 
	</update>

	<update id="updateIgnoreNullAdvisoryPricing">
		update z_advisory_pricing
		<set>
		<if test="id != null">
			id=#{id}, 
		</if>
		<if test="appId != null">
			app_id=#{appId}, 
		</if>
		<if test="consultantId != null">
			consultant_id=#{consultantId}, 
		</if>
		<if test="moldeId != null">
			molde_id=#{moldeId}, 
		</if>
		<if test="fixType != null and fixType != ''">
			fix_type=#{fixType}, 
		</if>
		<if test="unit != null and unit != ''">
			unit=#{unit}, 
		</if>
		<if test="originalPrice != null">
			original_price=#{originalPrice}, 
		</if>
		<if test="currentPrice != null">
			current_price=#{currentPrice}, 
		</if>
		<if test="introduction != null and introduction != ''">
			introduction=#{introduction}, 
		</if>
		<if test="status != null">
			status=#{status}, 
		</if>
		<if test="state != null">
			state=#{state}, 
		</if>
		<if test="validityStartTime != null">
			validity_start_time=#{validityStartTime}, 
		</if>
		<if test="validityEndTime != null">
			validity_end_time=#{validityEndTime}
		</if>
		</set>
		where id=#{id} 
	</update>

	<update id="updateByExampleAdvisoryPricing">
		update z_advisory_pricing
		<set>
			<if test="id != null">
			id=#{id}, 
			</if>
			<if test="appId != null">
			app_id=#{appId}, 
			</if>
			<if test="consultantId != null">
			consultant_id=#{consultantId}, 
			</if>
			<if test="moldeId != null">
			molde_id=#{moldeId}, 
			</if>
			<if test="fixType != null and fixType != ''">
			fix_type=#{fixType}, 
			</if>
			<if test="unit != null and unit != ''">
			unit=#{unit}, 
			</if>
			<if test="originalPrice != null">
			original_price=#{originalPrice}, 
			</if>
			<if test="currentPrice != null">
			current_price=#{currentPrice}, 
			</if>
			<if test="introduction != null and introduction != ''">
			introduction=#{introduction}, 
			</if>
			<if test="status != null">
			status=#{status}, 
			</if>
			<if test="state != null">
			state=#{state}, 
			</if>
			<if test="validityStartTime != null">
			validity_start_time=#{validityStartTime}, 
			</if>
			<if test="validityEndTime != null">
			validity_end_time=#{validityEndTime}
			</if>
		</set>
		where 1=1
		<include refid="conds" />
	</update>
	
	<select id="loadAdvisoryPricing" resultType="com.roof.advisory.advisorypricing.entity.AdvisoryPricingVo">
		select 
		<include refid="columns_as"/>,
		(SELECT NAME FROM z_consultant WHERE id = consultant_id ) as consultantName
		from z_advisory_pricing
		where id=#{id} 
	</select>
	
	<select id="selectAdvisoryPricing" resultType="com.roof.advisory.advisorypricing.entity.AdvisoryPricingVo">
		select 
		<include refid="columns_as"/>
		from z_advisory_pricing
		where 1=1
		<include refid="conds" />
		and state = '1'
	</select>
</mapper>