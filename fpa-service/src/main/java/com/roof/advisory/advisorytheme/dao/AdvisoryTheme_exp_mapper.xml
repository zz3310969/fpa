<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.roof.advisory.advisorytheme.dao">
	<sql id="columnsAs">
		t1.id as id, t1.app_id as appId, t1.name as name, t1.code as code, t1.remark as remark, t1.status as status, t1.state as state
	</sql>
	
	<select id="selectAdvisoryThemePage" resultType="com.roof.advisory.advisorytheme.entity.AdvisoryThemeVo">
		select
		<include refid="columnsAs"/>,
		(SELECT NAME FROM z_application WHERE id = t1.app_id ) as appName
		from
		z_advisory_theme t1
		join
		(SELECT
		id
		from
		z_advisory_theme
		where 1=1
		<include refid="conds" />
		and state = '1'
		order by id desc
		limit #{firstrownum}, #{limit}) t2
		where t1.id = t2.id
	</select>
	
	<select id="selectAdvisoryThemeCount" resultType="java.lang.Long">
		select
		count(id)
		from z_advisory_theme
		where 1=1
		<include refid="conds" />
		and state = '1'
	</select>
</mapper>